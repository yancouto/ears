language: rust
before_install:
  - sudo apt-get update
install:
  - sudo apt-get install libopenal-dev
  - sudo apt-get install libsndfile1-dev
env:
 - RUST_TEST_TASKS=1
script:
  - rustc --version
  - cargo build
  - cargo doc
